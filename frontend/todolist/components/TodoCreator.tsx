import React from 'react'
import { styled } from '@mui/system'

const TodoCreatorStyle = styled('div')`
	flex: 1;
`

const TodoCreator = () => {
	return <TodoCreatorStyle></TodoCreatorStyle>
}

export default TodoCreator
