{
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.4"
  },
  "scripts": {
    "prepare": "husky install",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "npm run format",
      "eslint --fix"
    ]
  }
}
